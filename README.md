# unit-red
Creates failing PHPUnit testfiles

Run the script in the 'src' or project root directory.

The script walks the 'src' directory and makes files and subdirectories in the 'tests'
directory that match 'src'.
